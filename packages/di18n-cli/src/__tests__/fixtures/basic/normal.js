import { intl } from 'di18n-react';
import React from 'react';

export default function Test() {
  return (
    <div>
      {/* this is a comment in JSX */}
      <Button
        onClick={admin.name && updateCardUserInfo.bind(this, admin.name)}
        className="啦啦啦呼呼呼"
      >
        {intl.t('拷贝')}
      </Button>
    </div>
  );
}
